replicaCount: 1

image:
  repository: ghcr.io/openstreetlifting/backend
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

containerPort: 8080

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

env: []
#  - name: DATABASE_URL
#    valueFrom:
#      secretKeyRef:
#        name: backend-secrets
#        key: database-url
