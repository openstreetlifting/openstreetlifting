info:
  name: list_ranking_years
  type: http
  seq: 5
  tags:
    - rankings

http:
  method: GET
  url: "{{baseUrl}}/api/v1/rankings/years"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: Distinct competition years, most recent first
    request:
      url: "{{baseUrl}}/api/v1/rankings/years"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            0
          ]
